# Etch Language Implementation Progress

Use `nim r src/etch.nim --run examples/hello.etch` to test
**NEVER CHANGE DIRECTORY**
**When you create new tests (but passing and failing) add the corresponding validation files .result and .error**

// Stress test failure: Loop with insufficient bounds check

fn testLoopInsufficientCheck() -> int {
  let arr: array[int] = [0, 1, 2, 3, 4];

  // Loop goes beyond array bounds
  for i in 0..<10 {
    return arr[i];  // ❌ Should fail! i ranges [0,9] but array is [0,4]
  }

  return -1;
}

fn main() -> void {
  print(string(testLoopInsufficientCheck()));
}

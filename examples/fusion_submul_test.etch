// Test SubMul fusion: a - b * c -> opSubMul
fn main() {
  let a = 100;
  let b = 5;
  let c = 10;

  // This should fuse into a single opSubMul instruction
  let result = a - b * c;

  print(result);  // Should print 50 (100 - 50)
}

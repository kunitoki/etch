// Test object types
type Person = object {
    name: string;
    age: int = 0;
};

fn test(p: Person) -> string {
    return p.name;
}

fn main() {
    let p: Person = { name: "Alice", age: 25 };
    print(p.name);
    print(p.age);
    print(test(p));
    print(p.test());
}
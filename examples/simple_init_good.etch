fn testInit(condition: bool) -> void {
    var x: int;

    if condition {
        x = 42;
    } else {
        x = 99;
    }

    print(x);  // This should be fine - x is always initialized
}

fn main() -> void {
    seed(42);
    testInit(rand(1) == 0);
}

import ffi clib/mathlib {
  fn c_add(a: int, b: int) -> int;
  fn c_multiply(a: int, b: int) -> int;
  fn c_power(base: int, exp: int) -> int;
}

fn main() {
  print("C FFI infrastructure is in place");

  let sum = c_add(10, 20);
  let product = c_multiply(5, 6);
  print(sum);
  print(product);
}

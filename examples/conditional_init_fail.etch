fn test(condition: bool) -> void {
    var x: int;
    
    if condition {
        x = 42;
    }
    
    print(x);  // This should fail - x may not be initialized
}

fn main() -> void {
    test(true);
}